spring.application.name=reading_groups

# Token secret
jwt.secret=test-secret-key

# Public endpoints
app.public.paths=/api/users/register,/api/auth/login, /api/auth/refresh, /api/auth/logout, /swagger-ui/**,/v3/api-docs/**

# File storage root
app.storage.root=./target/test-uploads
